<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="org.cabbage.codedemo.route.mapper.ext.BetaThirdExtMapper">

    <!-- Beta三方故障 独有查询：三方故障占比 -->
    <select id="queryThirdPartyRate" resultType="java.lang.String">
        SELECT CONCAT(table_name, '-queryThirdPartyRate') AS result
        FROM information_schema.TABLES
        WHERE table_schema = DATABASE()
          AND table_name = 'fault_detail'
        LIMIT 1
    </select>

</mapper>
